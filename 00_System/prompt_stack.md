# MOSA Prompt Stack

- Public template initialized. Add durable project decisions here as short summaries.
- Legacy maintenance and DAG validator ideas are folded into `00_System/mosa_cli.js` as read-only `dag` and `maintain` commands; registry mutation remains a confirmed Distiller workflow.

